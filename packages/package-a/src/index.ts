import { Car, User } from '@core/types';

const mustang: Car = { model: 'mustang', make: 'ford', year: 2020 }

const user: User = { avatarUrl: 'https://picsum.photos/800/600/?random', username: 'intrepidsovereign' }
