import { Car, User } from '@core/types';

const mustang: Car = { model: 'toyota', make: 'camry', year: 2016 }

const user: User = { avatarUrl: 'https://picsum.photos/800/600/?random', username: 'twisted-vortex' }
